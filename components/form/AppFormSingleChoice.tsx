import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppFormSingleChoice = () => {
  return (
    <View>
      <Text>AppFormSingleChoice</Text>
    </View>
  );
};

export default AppFormSingleChoice;

const styles = StyleSheet.create({});
