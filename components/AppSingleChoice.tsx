import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AppSingleChoice = () => {
  return (
    <View>
      <Text>AppSingleChoice</Text>
    </View>
  );
};

export default AppSingleChoice;

const styles = StyleSheet.create({});
